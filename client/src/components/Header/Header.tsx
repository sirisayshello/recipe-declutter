import styled from "@emotion/styled";

let Wrapper = styled.div({
  textAlign: "center",
});

export const Header = () => {
  return (
    <Wrapper>
      <h1>Recipe Declutter</h1>
    </Wrapper>
  );
};
